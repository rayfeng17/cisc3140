Check if an input string is a palindrome, return a boolean value. Allow list input type. Required implementation: Write your own reverser 
function.

(define isPalindrome?
(lambda (input-str)
  (letrec ((analyzer
             (lambda (init end)
               (let ((first-letter (string-ref input-str init))
                      (last-letter (string-ref input-str end)))
                 (cond
                  ((<= end init) #t)
                  ((not (char-alphabetic? first-letter))
                   (analyzer (+ init 1) end))
                  ((not (char-alphabetic? last-letter))
                   (analyzer init (- end 1)))
                  ((char-ci=? first-letter last-letter)
                   (analyzer (+ init 1) (- end 1)))
                  (else #f))))))
    (analyzer 0 (- (string-length input-str) 1))))) 
